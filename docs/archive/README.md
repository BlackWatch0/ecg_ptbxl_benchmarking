# 历史文档归档

本目录保存旧 Colab、EMD 和消融指南，仅用于追溯当时的环境、路径和实验假设。

- 内容未更新为当前 taskmanager、AWS/EBS 或数据资产契约。
- 文中的根目录 Shell 已移至 `scripts/legacy/`，相对路径和命令不再有效。
- 文中的 Google Drive、Colab、依赖版本和下载链接可能失效。
- EMD 当前为 `source_required`，旧指南不能证明 EMD 工作流可运行。
- 当前操作说明以根 `README.md` 和 `docs/` 下非 `archive/` 文档为准。
